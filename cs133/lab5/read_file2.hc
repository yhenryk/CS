
#include "Common.h"
void read_file2( int i, char* file_name_20, Context* context){
	/* This is suggested code - fix namings to match the data got if tag functions are missing or dependent on the inputs
	char* string_21;
	// allocate memory if necessary and fill in values to put here

	char* tagstring_21 = createTag(1, i);
	Put(string_21, tagstring_21, context->string_2);
	*/

}


